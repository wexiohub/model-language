{
  "name": "edge: null array elements resolve via default inside a loop",
  "template": "{{for x in xs}}[{{x | default: \"-\"}}]{{/for}}",
  "schema": [],
  "data": { "xs": [1, null, 3] },
  "expect": { "output": "[1][-][3]", "warnings": [] }
}
